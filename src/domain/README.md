# src/domain

Доменные модели и бизнес-сущности предметной области: скважина,
групповая установка, замер дебита и т.д.

Модели здесь не зависят от источника данных и от способа хранения —
это ядро, вокруг которого строятся расчёты (`src/calc/`) и API
(`src/api/`).
