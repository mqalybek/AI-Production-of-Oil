# tests

Тесты pytest. Структура зеркалит `src/` (например, `tests/calc/` для
тестов `src/calc/`).

Расчётные модули тестируются числовыми примерами, которые можно
проверить вручную — не просто моками.
